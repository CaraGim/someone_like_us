# prototype warning for development

The background: 
 Since this is on going development, lots of asset for graphics, UI, sound effect etc are not presenting for build. However, this prototype has the purpose that programming test and how the game structure will be if it's following the simplified GDD.

Therefore, the asset will be added if the game structure is completed. 
If there is no Graphic asset for UI, then make the mock UI graphic button as much as simple.
If there is no sound, then generate simple beeping audio for empty sound effect. For BGM, no alternative audio, so just let it leave as silence.


Conclusion:
 Build real logic, but stub the presentation layer — primitive-shape UI buttons instead of art, a short procedural beep instead of missing SFX, silence instead of missing BGM.